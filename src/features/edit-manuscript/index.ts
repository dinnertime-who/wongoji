export { useManuscriptDoc } from "./model/use-manuscript-doc";
/*
 * 에디터는 **늦게 받는 것**이 내보내진다(`LazyEditor`). 무거운 쪽(TipTap)을 첫
 * 짐에서 빼기 위한 것이고, 부르는 쪽은 달라지지 않는다.
 */
export { WongojiEditor } from "./ui/LazyEditor";
