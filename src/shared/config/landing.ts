/**
 * 홈이 스스로를 설명하는 말.
 *
 * **읽는 데가 둘이라 여기 모아 둔다.** 화면이 그리고(`shared/ui/landing-intro`),
 * 그 옆에서 라우트가 같은 배열로 FAQ 구조화 데이터를 짓는다(`routes/index.tsx`).
 * 구글은 FAQ 구조화 데이터가 **쪽에 실제로 보이는 글과 같을 것**을 요구하므로,
 * 두 곳에 따로 적어 두면 한쪽만 고쳤을 때 규정 위반이 된다 — 그것도 배포하고
 * 몇 주 뒤에 서치 콘솔에서나 알게 된다.
 *
 * ---
 *
 * 이 글이 왜 필요한가.
 *
 * `/`는 **크롤러에게 빈 쪽이었다.** 비로그인으로 들어오면 원고 한 편이 그대로
 * 열리는데, 그 화면에 있는 글자라고는 빈 에디터의 안내문과 원고지 격자뿐이다.
 * "원고지 작성 사이트"로 검색한 사람에게 보이려면 그 말이 쪽 안에 있어야 하는데
 * 메타 태그에만 있었다 — 메타는 **이미 찾은 쪽을 어떻게 소개할지**를 정할 뿐,
 * 없는 본문을 대신해 주지 않는다.
 *
 * 그래서 원고지 아래에 읽을 것을 둔다. 쓰는 자리를 밀어내지 않도록 **접히는 곳
 * 아래**에 두었다 — 글을 쓰러 온 사람은 여기까지 내려올 일이 없다.
 */

/** 검색 결과에서 이 쪽을 찾게 될 사람이 쓴 말. 쪽의 h1이 된다 */
export const LANDING_HEADING = "온라인 원고지 작성 사이트";

/** 제목 아래 한 문단. 무엇을 하는 곳인지 한 번에 */
export const LANDING_LEAD =
	"원고지는 브라우저에서 바로 쓰는 온라인 원고지입니다. 내려받거나 설치할 프로그램이 없고, 글을 쓰면 200자 원고지 규칙에 맞춰 실시간으로 조판되며 매수가 함께 계산됩니다. 로그인 없이 한 편을 그대로 쓸 수 있습니다.";

export interface Feature {
	/** 목록에서 굵게 나오는 이름 */
	title: string;
	/** 한 줄 설명. **하는 일만 적는다** — 확인되지 않은 것은 여기 오지 않는다 */
	body: string;
}

/**
 * 이 서비스가 실제로 하는 일.
 *
 * **앱에 없는 것은 적지 않는다.** 검색에 걸리려고 기능을 부풀리면 들어온 사람이
 * 곧바로 나가고, 그 이탈이 다시 순위를 깎는다. 여기 여섯 줄은 전부 지금 화면에서
 * 눌러 볼 수 있는 것들이다.
 */
export const FEATURES: Feature[] = [
	{
		title: "실시간 원고지 조판",
		body: "글을 치는 대로 200자 원고지(20칸 × 10줄)에 옮겨 그립니다. 띄어쓰기가 줄 첫 칸에 오지 않는 것, 문장부호가 앞 칸에 합쳐 들어가는 것까지 규칙대로 처리합니다.",
	},
	{
		title: "매수·글자수 자동 계산",
		body: "지금까지 몇 자를 썼고 원고지로 몇 매인지 늘 보입니다. 목표 매수를 적어 두면 남은 분량이 진행률로 나옵니다.",
	},
	{
		title: "설치 없이, 무료로",
		body: "웹 브라우저만 있으면 됩니다. 내려받을 프로그램도, 결제도 없습니다.",
	},
	{
		title: "휴대폰에서도",
		body: "좁은 화면에서는 원고와 원고지를 단추 하나로 오갑니다. 지하철에서 쓴 글을 집에서 이어 쓸 수 있습니다.",
	},
	{
		title: "워드(.docx)로 내보내기",
		body: "제출용 파일로 받습니다. 바탕체 11pt, 줄간격 160%, A4 문서로 만들어집니다. 텍스트(.txt)로 받거나 쓰던 글을 불러올 수도 있습니다.",
	},
	{
		title: "여러 편과 폴더는 로그인 후에",
		body: "로그인하면 원고를 여러 편 두고 폴더로 갈라 보관합니다. 지운 것은 휴지통에 남고, 고치기 전으로 되돌리는 이력도 있습니다.",
	},
];

export interface Question {
	/** 물음. 검색창에 그대로 쳐 넣을 법한 말로 */
	q: string;
	/**
	 * 답. **평문이어야 한다** — 이 문자열이 그대로 구조화 데이터로 나간다.
	 * 링크를 걸고 싶으면 화면 쪽에서 따로 붙인다.
	 */
	a: string;
}

/**
 * 자주 묻는 것.
 *
 * 고른 기준은 **검색창에 실제로 쳐 넣는 말**이다. "원고지 작성 프로그램"으로
 * 찾는 사람이 정말 알고 싶은 것은 "설치해야 하나"이고, "원고지 온라인"으로 찾는
 * 사람은 "가입해야 하나"를 묻는다. 그 말이 물음 자리에 그대로 있어야 걸린다.
 */
export const FAQ: Question[] = [
	{
		q: "원고지 작성 프로그램을 설치해야 하나요?",
		a: "설치하지 않아도 됩니다. 웹 브라우저에서 바로 쓰는 온라인 원고지라, 이 쪽을 연 것만으로 준비가 끝났습니다. 윈도우·맥·휴대폰 어디서든 같은 주소로 들어옵니다.",
	},
	{
		q: "회원가입이나 로그인이 필요한가요?",
		a: "원고 한 편을 쓰는 데에는 필요하지 않습니다. 지금 화면에 바로 쓰면 됩니다. 원고를 여러 편 두거나 폴더로 갈라 보관하려면 그때 로그인하면 됩니다.",
	},
	{
		q: "원고지 매수는 어떻게 세나요?",
		a: "쓴 글자 수가 아니라 원고지에 조판된 칸을 세어 매수를 냅니다. 한글(HWP)이 원고지 매수를 세는 것과 같은 방식이라, 글자수로 어림잡을 때와 숫자가 달라집니다. 대화가 많은 소설일수록 차이가 큽니다.",
	},
	{
		q: "쓴 원고를 파일로 낼 수 있나요?",
		a: "워드(.docx)와 텍스트(.txt) 두 가지로 내보냅니다. 워드 파일은 바탕체 11pt, 줄간격 160%, A4 문서로 만들어집니다. 공모전마다 서식이 다르니 요강을 함께 확인하세요.",
	},
	{
		q: "쓰던 글이 사라지지 않나요?",
		a: "로그인하지 않고 쓴 원고 한 편은 이 브라우저에 남아, 창을 닫았다 열어도 이어서 쓸 수 있습니다. 로그인해서 쓴 원고는 서버에 저장되어 다른 기기에서도 열립니다.",
	},
	{
		q: "원고지 쓰는 법도 볼 수 있나요?",
		a: "칸 배치와 문단 들여쓰기, 띄어쓰기와 줄바꿈, 문장부호, 공모전 매수 계산을 네 편의 글로 정리해 두었습니다. 이 쪽 아래 사용법 목록에서 갑니다.",
	},
];
