/**
 * 이 서비스가 바깥에 어떻게 소개되는가.
 *
 * 검색 결과의 제목·설명, 링크를 나눌 때 딸려 가는 그림이 여기 모여 있다.
 * `__root`가 문서 머리에 얹고, `index`가 canonical을 얹는다 — 같은 문구를
 * 두 곳에 적어 두면 한쪽만 고치게 된다.
 */

/**
 * 배포된 곳.
 *
 * **절대 URL이어야 한다.** canonical·Open Graph·JSON-LD를 읽는 것은 이 문서를
 * 받아 간 남의 서버(구글·카카오·트위터)라, 상대 경로에는 기준이 없다.
 */
export const SITE_URL = "https://wongo.dinnertimes.app";

/**
 * 검색 결과에 걸리는 제목. 원고를 쓰는 동안의 탭 제목은 `_app`이 짧게 덮는다.
 *
 * **앞머리에 사람이 검색창에 치는 말을 둔다.** 구글은 한글 제목을 서른 자 남짓에서
 * 자르므로, 뒤에 붙은 것은 못 읽는다고 보는 편이 낫다. "온라인 원고지 작성 사이트"
 * 하나가 `원고지` · `원고지 작성` · `원고지 작성 사이트` · `원고지 사이트` ·
 * `원고지 온라인`을 함께 문다 — 다섯 개를 따로 늘어놓는 것보다 짧고 읽기도 낫다.
 */
export const SITE_TITLE =
	"원고지 - 온라인 원고지 작성 사이트 | 200자 원고지 조판·매수 계산";

/**
 * 제목 아래 딸려 나오는 설명.
 *
 * "설치 없이"와 "회원가입 없이"를 앞에 둔다. `원고지 작성 프로그램`으로 찾는
 * 사람이 정말 재고 있는 것이 그 둘이라, 검색 결과에서 이미 답이 보여야 누른다.
 */
export const SITE_DESCRIPTION =
	"설치 없이 브라우저에서 바로 쓰는 온라인 원고지입니다. 글을 쓰면 200자 원고지 규칙대로 실시간 조판되고 매수가 자동으로 계산됩니다. 회원가입 없이 원고 한 편을 쓸 수 있고, 공모전·신춘문예 제출용 워드(.docx) 내보내기를 지원합니다.";

/**
 * SNS 미리보기에 들어가는 설명.
 *
 * 검색용 설명과 따로 두는 이유는 자리가 다르기 때문이다 — 카카오톡·트위터의
 * 미리보기 카드는 두 줄 남짓에서 잘린다.
 */
export const SITE_SHARE_DESCRIPTION =
	"설치 없이 브라우저에서 쓰는 온라인 원고지 — 200자 원고지 규칙대로 실시간 조판";

/** 링크를 나눌 때 딸려 가는 그림. `public/og-image.png`, 1200×630 */
export const SITE_OG_IMAGE = `${SITE_URL}/og-image.png`;

/**
 * 검색어. 상위 몇 개 말고는 요즘 엔진이 보지 않지만, 네이버가 아직 읽는다.
 *
 * 그래서 **앞자리를 노리는 말에 준다.** 뒤쪽은 네이버가 읽어 주면 좋고 아니어도
 * 그만인 자리다.
 */
export const SITE_KEYWORDS =
	"원고지, 원고지 작성, 원고지 작성 사이트, 원고지 작성 프로그램, 원고지 온라인, 온라인 원고지, 원고지 사이트, 200자 원고지, 원고지 쓰는 법, 원고지 양식, 원고지 조판, 공모전 원고지, 신춘문예 매수 계산, 글자수 세기";
