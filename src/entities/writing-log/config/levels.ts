/**
 * 진하기를 가르는 자리. **원고지의 매수로 센다.**
 *
 * 글자 수를 그냥 나누지 않는 이유는, 이 앱을 쓰는 사람이 제 분량을 매로 세기
 * 때문이다. 200자가 한 매다 — "다섯 매 썼다"가 이 사람들의 하루 단위다.
 *
 * 아래 칸은 두지 않는다. **손댔으면 무조건 한 칸은 심긴다.** 그것이 이 잔디가
 * GitHub과 다른 점이다 — 글을 줄이는 날이 일하는 날에 든다.
 */
export const LEVELS = [
	/** 한 매 — 손은 댔다 */
	200,
	/** 다섯 매 */
	1000,
	/** 열 매 — 원고지로 하루치라 할 만한 분량 */
	2000,
] as const;
