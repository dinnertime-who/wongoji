/**
 * 보관함의 한계값.
 *
 * 화면과 규칙이 함께 보는 숫자라 한 곳에 둔다.
 *
 * 전에는 여기에 `SHEET_LIMIT`(100매)이 함께 있었다. 머리말에 "보관함 12 / 100매"를
 * 적어 두고 넘으면 색을 바꾸던 것인데, **우리가 지어낸 숫자였다** — 막지도 않았고
 * 브라우저가 실제로 거절하는 지점과도 아무 관계가 없었다. 정말로 저장이 거절될
 * 때의 길은 따로 있다(`safeSetItem`의 quota 결과 → `SaveErrorBanner`).
 */

/** 휴지통에 머무는 기간 */
export const TRASH_DAYS = 30;
